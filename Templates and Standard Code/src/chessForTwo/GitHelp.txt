Normal Process (If no conflicts)

Commit Local (multibple commits are ok)
Pull -- from Git Repo
if no conflicts: push upstream


if conflicts: 
resolve manually
add affected .java's to index
from git perspective commit and push
